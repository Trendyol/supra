enum CONTENT_TYPE {
  TextEventStream = 'text/event-stream',
  ApplicationJson = 'application/json'
}

enum CACHE_CONTROL {
  NoCache = 'no-cache'
}

enum CONNECTION {
  KeepAlive = 'keep-alive'
}

export {
  CONNECTION,
  CACHE_CONTROL,
  CONTENT_TYPE
}
